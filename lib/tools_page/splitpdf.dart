import 'package:flutter/material.dart';
class spiltpdf extends StatelessWidget {
  const spiltpdf({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
