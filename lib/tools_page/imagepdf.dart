import 'package:flutter/material.dart';

class imagepdf extends StatelessWidget {
  const imagepdf({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
