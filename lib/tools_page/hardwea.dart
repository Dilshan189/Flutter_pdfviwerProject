import 'package:flutter/material.dart';

class hardwea extends StatelessWidget {
  const hardwea({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
